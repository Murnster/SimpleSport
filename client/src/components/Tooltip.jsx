import Reach, { useState } from 'react';
import "../css/Tooltip.css";

const Tooltip = () => {
    return (
        <div id="tooltipContent" className='tooltip-tip'></div>
    )
};

export default Tooltip;